import colors from './colors';

export default {
  title: { color: colors.white, fontSize: 24 },
  basicText: { color: colors.white },
  highlightText: { color: colors.gold },
  timeText: { color: colors.grey },
};
